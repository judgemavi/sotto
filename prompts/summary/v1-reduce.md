Merge the JSON recaps from consecutive windows into one factual recap with exactly the same schema.
Return JSON only. Preserve and combine timeline event citations. Deduplicate claims. Recalculate talk-time approximately across windows. Mark an objection resolved only if a cited later-window claim actually resolves it. Never invent a citation.
